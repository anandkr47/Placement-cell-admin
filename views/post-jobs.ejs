<!-- job_post.ejs -->

<!-- job_post.ejs -->

<style>
    .container {
      max-width: 600px;
      margin: 0 auto;
    }
  
    .mt-4 {
      margin-top: 1.5rem;
    }
  
    .job-post-container {
      background-color: #f2f2f2;
      padding: 1.5rem;
      border-radius: 5px;
    }
  
    .job-post-form {
      margin-top: 1.5rem;
    }
  
    .form-group {
      margin-bottom: 1rem;
    }
  
    label {
      font-weight: bold;
      color: black; /* Set label color to white */
    }
  
    input[type="text"],
    textarea,
    input[type="date"] {
      width: 100%;
      padding: 0.5rem;
      border: 1px solid #ccc;
      border-radius: 5px;
      font-size: 1rem;
      color: #333;
    }
  
    textarea {
      resize: vertical;
    }
  
    .submit-btn {
      background-color: #4caf50;
      color: white;
      padding: 0.5rem 1rem;
      border: none;
      border-radius: 5px;
      font-size: 1rem;
      cursor: pointer;
    }
  
    .table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 1.5rem;
    }
  
    th,
    td {
      padding: 0.5rem;
      text-align: left;
      border-bottom: 1px solid #ddd;
      font-size: 1rem;
    }
  
    th {
      background-color: #f2f2f2;
    }
  
    .btn {
      padding: 0.3rem 0.6rem;
      border: none;
      border-radius: 3px;
      cursor: pointer;
      font-size: 0.9rem;
      text-decoration: none;
      text-align: center;
      transition: background-color 0.3s ease;
    }
  
    .btn-primary {
      background-color: #007bff;
      color: white;
    }
  
    .btn-primary:hover {
      background-color: #0056b3;
    }
  
    .btn-danger {
      background-color: #dc3545;
      color: white;
    }
  
    .btn-danger:hover {
      background-color: #b30018;
    }
  
    .no-jobs{
      color: #777;
      font-style: italic;
      margin-top: 1rem;
    }
    h2{
        color: slateblue;
    }
    .job-listing {
    margin-top: 20px;
  }

  .table {
    width: 100%;
    border-collapse: collapse;
  }

  .table th,
  .table td {
    padding: 10px;
    text-align: left;
    border-bottom: 1px solid #ddd;
    color: white;
  }

  .table th {
    font-weight: bold;
  }

  .btn {
    display: inline-block;
    padding: 8px 12px;
    border-radius: 4px;
    text-decoration: none;
    color: #fff;
    
    border: none;
    cursor: pointer;
  }

  .btn-primary {
    background-color: #007bff;
  }

  .btn-danger {
    background-color: #dc3545;
  }

  .actions {
    display: flex;
    gap: 5px;
  }

  .no-jobs {
    margin-top: 20px;
    color: #888;
    text-align: center;
  }
  </style>
  
  <!-- Rest of the HTML code remains the same -->
  
  
  <!-- Rest of the HTML code remains the same -->
  
  <!-- Rest of the HTML code remains the same -->
  

<div class="container mt-4">
  <h2>Create Job Post</h2>

  <div class="job-post-container">
    <form class="job-post-form" action="/job/create" method="POST">
      <div class="form-group">
        <label for="title">Job Title</label>
        <input type="text" id="title" name="title" required>
      </div>

      <div class="form-group">
        <label for="company">Company</label>
        <input type="text" id="company" name="company" required>
      </div>

      <div class="form-group">
        <label for="location">Location</label>
        <input type="text" id="location" name="location" required>
      </div>

      <div class="form-group">
        <label for="description">Job Description</label>
        <textarea id="description" name="description" required></textarea>
      </div>

      <div class="form-group">
        <label for="requirements">Requirements</label>
        <textarea id="requirements" name="requirements" required></textarea>
      </div>

      <div class="form-group">
        <label for="contact">Contact Information</label>
        <input type="text" id="contact" name="contact" required>
      </div>

      <div class="form-group">
        <label for="deadline">Application Deadline</label>
        <input type="date" id="deadline" name="deadline" required>
      </div>

      <button type="submit" class="submit-btn">Post Job</button>
    </form>
  </div>
  <% if (jobs.length > 0) { %>
    <div class="job-listing">
      <h2>Posted Jobs</h2>
      <table class="table mt-4">
        <thead>
          <tr>
            <th>Job Title</th>
            <th>Company</th>
            <th>Location</th>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody>
          <% jobs.forEach((job) => { %>
            <tr>
              <td><%= job.title %></td>
              <td><%= job.company %></td>
              <td><%= job.location %></td>
              <td>
                <div class="actions">
                  <a href="/job/<%= job._id %>/edit" class="btn btn-primary">Edit</a>
                  <form action="/job/<%= job._id %>/delete" method="POST" class="d-inline">
                    <button type="submit" class="btn btn-danger">Delete</button>
                  </form>
                </div>
              </td>
            </tr>
          <% }); %>
        </tbody>
      </table>
    </div>
  <% } else { %>
    <div class="no-jobs">
      <p>No jobs posted yet.</p>
    </div>
  <% } %>
  